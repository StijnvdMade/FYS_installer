# Make script an executable
sudo chmod +x ./access_point_installer.sh
sudo chmod +x ./captive_portal_installer.sh
sudo chmod +x ./firewall_installer.sh

# Run the install scripts
sudo ./access_point_installer.sh
sudo ./captive_portal_installer.sh
sudo ./firewall_installer.sh